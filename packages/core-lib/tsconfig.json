{
  "extends": "../../tsconfig.base.json", // Inherit from root base
  "compilerOptions": {    
    "declaration": true,
    "composite": true, // Useful in monorepos with project references if needed
    "rootDir": "src",
  },
  "include": ["src/**/*.ts","src/**/*.tsx"], // Only include source files
  "exclude": ["node_modules", "dist"] // Exclude build output and deps
}